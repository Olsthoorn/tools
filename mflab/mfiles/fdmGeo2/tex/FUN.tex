\begin{alltt}
\textcolor{keyword}{function} e = FUN(p)
\textcolor{comment}{% used with simple calibration (modelScript5), called by lsqnonlin}
\textcolor{keyword}{global} eMeas Q T0 S0 r

fprintf(\textcolor{string}{'.'}); \textcolor{comment}{% shows each call by printing a dot}

t = eMeas(:,1);

\textcolor{comment}{% Parameter update}
T = exp(p(1)) * T0;
S = exp(p(2)) * S0;

\textcolor{comment}{% compute drawdown (would normally be hte model)}
s = Q/(4*pi*T) * expint( r\^{}2 *S./(4*T*t));

\textcolor{comment}{% output difference with measurements}
e = s(:) - eMeas(:,2);
\end{alltt}
